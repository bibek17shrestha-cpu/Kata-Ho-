Drop these two files here — the HTML already references them:

- favicon.png   (recommend 64x64 or 32x32, square)
- icon-192.png  (192x192, used for "add to home screen" / apple-touch-icon)

Once added, they'll show up automatically — no other code changes needed.
